enum LoginUserType { user, company }

enum UserLoginType{
  manual,social
}
enum TypeAuth { user, company, provider }

enum SocialLoginType { google, facebook, apple }

enum CompanyOwnerType {citizen,nonCitizen}

enum VerifyByType{sms,email}

enum ServicePaymentType{
  paid,
  free
}

enum PaymentMethod { cash, wallet }

enum ProviderStatus{
  online,
  busy,
  offline
}


